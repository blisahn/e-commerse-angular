/**
 * We are creating a new class because we want to have another Functionality in our codebase
 * after creating and initilizing with the specified fields from the rest endpoints , we are creating a new component 
 * about feature that we want
 */
export class ProductCategory {
  constructor(public id: number,
              public categoryName: string) {}
}
